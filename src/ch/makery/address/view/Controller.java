package ch.makery.address.view;

import javafx.fxml.FXML;
import javafx.scene.control.ComboBox;

public class Controller {
	
	@FXML
	private ComboBox monthBox;
	
	@FXML
	private ComboBox dayBox;
	
	@FXML
	private ComboBox yearBox;
	
	@FXML
	private void loadDays(){

//		if (monthBox.getValue().toString().equals("February"))
//		{
//
//			if (yearBox.getValue() = !null) {
//
//				boolean leapYear = ((yearBox.getValue() % 4 == 0) && (yearBox.getValue() % 100 != 0) || (yearBox.getValue() % 400 == 0));
//
//				if (leapYear) { LOAD LIST OF 29 DAYS
//				
//				} else { LOAD LIST OF 28 DAYS }
//
//			} else { LOAD LIST OF 29 DAYS } -- Just in case they didn't select a year yet.
//  	}
//
//		if (monthBox.getValue().toString() = "January" || "March" || "May" || "July" || "August" || "October" || "December") {
//
//				LOAD LIST OF 31 DAYS
//				
//			} else { LOAD LIST OF 30 DAYS }
	}
	
	

}
